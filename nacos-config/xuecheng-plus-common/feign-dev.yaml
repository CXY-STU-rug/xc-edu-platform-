feign:
  client:
    config:
      default: # default全局的配置
        loggerLevel: BASIC # 日志级别，BASIC就是基本的请求和响应信息
        connectTimeout: 3000 # feign建立连接超时（与Ribbon对齐）
        readTimeout: 25000 # feign读响应超时（上传静态页等耗时操作需要秒级窗口）
  hystrix:
    enabled: false # v2.2 已迁移 Sentinel，Hystrix 停用（jar 仅保留 FallbackFactory 接口兼容）
  sentinel:
    enabled: true # Feign 调用异常时走 fallbackFactory 降级；信号量隔离不切线程，traceId 不断链
  httpclient:
    enabled: true # 开启feign对HttpClient的支持
    max-connections: 200 # 最大的连接数
    max-connections-per-route: 50 # 每个路径的最大连接数

# Sentinel 模式下没有外层熔断计时器，超时完全由 Ribbon/Feign 两层控制（保持对齐）
ribbon:
  ConnectTimeout: 3000 # 建立连接超时
  ReadTimeout: 25000 # 读响应超时
  MaxAutoRetries: 0 # 同实例不重试（上传类接口重试会放大副作用）
  MaxAutoRetriesNextServer: 0 # 不换实例重试
